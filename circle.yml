dependencies:
    pre:
        - sudo apt-get install autoconf-archive automake-1.14 autoconf2.59 build-essential check
test:
    pre:
        - ./autogen.sh
        - make
    override:
        - make check
